{
  "name": "mychael-pm-estudos",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble-published-catalog.mjs",
    "validate": "npm run assemble && node scripts/validate-content.mjs",
    "manifest": "node scripts/generate-manifest.mjs",
    "export:lot": "node scripts/export-lot.mjs",
    "deployment:check": "node scripts/check-public-deployment.mjs",
    "coverage:check": "node scripts/check-competition-coverage.mjs",
    "supabase:check": "node scripts/check-supabase.mjs",
    "test": "node --test",
    "test:e2e": "playwright test",
    "static": "node scripts/check-static.mjs",
    "check": "npm run validate && npm run coverage:check && npm run static && npm run supabase:check && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
