import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { validateCatalog } from '../scripts/content-rules.mjs';
const catalog=JSON.parse(await readFile(new URL('../content/catalog.json',import.meta.url),'utf8'));
const lot=JSON.parse(await readFile(new URL('../content/lots/lot-0052.json',import.meta.url),'utf8'));
const taxonomy=JSON.parse(await readFile(new URL('../content/lots/lot-0052-taxonomy.json',import.meta.url),'utf8'));
const applicability=JSON.parse(await readFile(new URL('../content/content-applicability.json',import.meta.url),'utf8'));
const find=(collection,id)=>collection.find((item)=>item.id===id);
const ids=['Q000441','Q000442','Q000443','Q000444','Q000445','Q000446','Q000447'];
const answers=['C','B','A','D','C','A','E'];
test('LOT-0052 permanece íntegro no catálogo v60 sem reduzir validações',()=>{ assert.deepEqual(validateCatalog(catalog),[]); assert.equal(lot.lotId,'LOT-0052'); assert.equal(lot.contentVersion,51); assert.equal(catalog.contentVersion,60); assert.equal(catalog.publication.lotId,'LOT-0061'); assert.equal(catalog.units.length,60); assert.equal(catalog.materials.length,60); assert.equal(catalog.questions.length,413); assert.equal(catalog.questionSets.length,60); });
test('U051 preserva fonte e separação teoria/questões',()=>{ const unit=find(catalog.units,'U051'); assert.ok(unit); assert.deepEqual(unit.coverage,['PMDF','PMGO']); assert.deepEqual(unit.materialIds,['M051']); assert.deepEqual(unit.questionIds,ids); assert.deepEqual(unit.questionSetIds,['QS051']); assert.deepEqual(unit.sourceIds,['FNT-0005']); const material=find(catalog.materials,'M051'); assert.ok(material); assert.equal(material.unitId,'U051'); assert.equal('questionIds' in material,false); assert.equal('answer' in material,false); ids.forEach((id,index)=>{ const q=find(catalog.questions,id); assert.ok(q,id); assert.equal(q.answer,answers[index],id); assert.equal(q.options.length,5,id); assert.ok(q.options.some((o)=>o.id===answers[index]),id); assert.deepEqual(q.sourceIds,['FNT-0005'],id); assert.equal(q.annulled,false,id); assert.equal(q.duplicateOf,null,id); assert.equal(q.imageRequired,false,id); assert.equal(q.valid,true,id); }); const set=find(catalog.questionSets,'QS051'); assert.ok(set); assert.deepEqual(set.questionIds,ids); assert.equal('questions' in set,false); assert.equal('answers' in set,false); });
test('taxonomia da polícia judiciária militar preserva relação pai-filho',()=>{ const parent=find(taxonomy.topics,'ASS-PPM-002'); const child=find(taxonomy.topics,'ASS-PPM-002-01'); assert.ok(parent); assert.ok(child); assert.equal(parent.subjectId,'MAT-PPM'); assert.equal(child.subjectId,'MAT-PPM'); assert.equal(child.parentId,'ASS-PPM-002'); });
test('U051 tem aplicabilidade explícita PMDF + PMGO sem ativar rotação específica',()=>{ const rule=applicability.unitApplicability.find((item)=>item.unitId==='U051'); assert.ok(rule); assert.equal(rule.classification,'shared-2'); assert.deepEqual(rule.competitions,['PMDF','PMGO']); assert.deepEqual(rule.evidenceIds,['FNT-0015','FNT-0014','FNT-0005']); assert.equal(applicability.specificRotation.enabled,false); });
