import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { validateCatalog } from '../scripts/content-rules.mjs';
const catalog=JSON.parse(await readFile(new URL('../content/catalog.json',import.meta.url),'utf8')); const lot=JSON.parse(await readFile(new URL('../content/lots/lot-0049.json',import.meta.url),'utf8')); const applicability=JSON.parse(await readFile(new URL('../content/content-applicability.json',import.meta.url),'utf8')); const find=(collection,id)=>collection.find((item)=>item.id===id); const ids=['Q000420','Q000421','Q000422','Q000423','Q000424','Q000425','Q000426']; const answers=['B','D','A','C','E','A','D'];
test('LOT-0049 permanece íntegro no catálogo cumulativo v54',()=>{assert.deepEqual(validateCatalog(catalog),[]); assert.equal(lot.lotId,'LOT-0049'); assert.equal(lot.contentVersion,48); assert.equal(catalog.contentVersion,54); assert.equal(catalog.publication.lotId,'LOT-0055'); assert.equal(catalog.units.length,54); assert.equal(catalog.materials.length,54); assert.equal(catalog.questions.length,371); assert.equal(catalog.questionSets.length,54);});
test('U048 preserva taxonomia, fontes e separação teoria/questões',()=>{assert.ok(find(catalog.topics,'ASS-RLM-001')); assert.ok(find(catalog.topics,'ASS-RLM-001-03')); const unit=find(catalog.units,'U048'); assert.ok(unit); assert.deepEqual(unit.coverage,['PMDF','PMMG']); assert.deepEqual(unit.materialIds,['M048']); assert.deepEqual(unit.questionIds,ids); assert.deepEqual(unit.questionSetIds,['QS048']); assert.deepEqual(unit.sourceIds,['FNT-0015','FNT-0025','FNT-0026']); const material=find(catalog.materials,'M048'); assert.ok(material); assert.equal(material.unitId,'U048'); assert.equal(material.type,'Aula teórica'); assert.equal('questionIds' in material,false); assert.equal('answer' in material,false); assert.equal('options' in material,false); ids.forEach((id,index)=>{const q=find(catalog.questions,id); assert.ok(q,id); assert.equal(q.unitId,'U048',id); assert.equal(q.answer,answers[index],id); assert.equal(q.options.length,5,id); assert.ok(q.options.some((o)=>o.id===answers[index]),id); assert.deepEqual(q.sourceIds,['FNT-0015','FNT-0025'],id); assert.ok(q.commentary.length>20,id); assert.ok(q.foundation.length>20,id); assert.equal(q.annulled,false,id); assert.equal(q.duplicateOf,null,id); assert.equal(q.imageRequired,false,id); assert.equal(q.valid,true,id);}); const set=find(catalog.questionSets,'QS048'); assert.ok(set); assert.equal(set.unitId,'U048'); assert.deepEqual(set.questionIds,ids); assert.equal(set.correctionMode,'Ao finalizar'); assert.equal('questions' in set,false); assert.equal('answers' in set,false);});
test('U048 tem aplicabilidade explícita PMDF + PMMG sem ativar PMGO',()=>{const rule=applicability.unitApplicability.find((item)=>item.unitId==='U048'); assert.ok(rule); assert.equal(rule.classification,'shared-2'); assert.deepEqual(rule.competitions,['PMDF','PMMG']); assert.deepEqual(rule.evidenceIds,['FNT-0015','FNT-0025','FNT-0026']); assert.equal(applicability.specificRotation.enabled,false);});
